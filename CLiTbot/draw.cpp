#include <iostream>
#include <string>
#include <algorithm>
#include <fstream>
#include <cmath>
#include "head.h"
using namespace std;

void save(const char* path) {
	return;
	 // 现在写法不对，应该是用给定的全局变量获取地图状态，
	 //调用这个函数的时候 
}
void auto_save(){
	return;
}